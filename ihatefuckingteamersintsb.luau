-- I hate fucking teamers, and this is only defense against those fuck shits, am I justified? Yes. I can only do so much as one person in a 1vX (while X <= 2) 

-- Objectives: Gonna keep it simple, just 3 m1s, 1st move, 3 m1s, second move, 3m1s third move, so on,  for each character. Some characters have counters. For now I will only make this for saitama, garou, maybe tatsumaki and.. sonic. THE STRONGEST BATTLEGROUNDS. 

-- WE using a library for the UI cuz im fucking lazy, and smart asf. I hate skids tbh, but yeah... So
-- We select a player, game orbits us around that player, and if we can.. implement some kind of auto block.. then it does 3 m1s, 1st move, 3 m1s, second move, 3m1s third move, then maybe the 4th, 4th move is always a counter for some characters like garou. WE will work around this. Sounds simple ain it, just spamming remotes and stuff... anyway 
--For now ima make it with only saitama in mind.
--[[
Example of a "move" got from cobalt.. pretty neat eh, shits easy, now just m1
-- This code was generated by Cobalt
-- https://github.com/notpoiu/cobalt

local Event = game:GetService("Players").LocalPlayer.Character.Communicate
Event:FireServer(
    {
        IsAutoActivate = true,
        Goal = "Console Move",
        Tool = game:GetService("Players").LocalPlayer.Backpack["Normal Punch"],
        ToolName = "Normal Punch"
    }
)

HOLY FUCKING SHIT. I spent 2 hours trying to figure out how to get this remote since this shitty emulator removed my mobile GUI. Ugh and its too simple. pisses me off

local character = game.Players.LocalPlayer.Character
local remote = character:FindFirstChild("Communicate")

if remote then
    remote:FireServer({
        Mobile = true,
        Goal = "LeftClick",
    })

    remote:FireServer({
        Goal = "LeftClickRelease",
        Mobile = true,
    })
end
]]--
local WindUI = loadstring(game:HttpGet(
    "https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"
))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local selectedTargetName = nil
local comboEnabled = false
local workerId = 0

local orbitConnection = nil
local orbitHumanoid = nil
local oldPlatformStand = false
local angle = 0

local ORBIT_RADIUS = 3
local ORBIT_HEIGHT = -6
local ORBIT_SPEED = 4

local M1_DELAY = 0.18
local TOOL_DELAY = 0.45
local ROUND_DELAY = 0.75
local DEAD_PAUSE_DELAY = 0.5

local function getCharacter(targetPlayer)
    if not targetPlayer then
        return nil
    end

    return targetPlayer.Character
end

local function getHumanoid(targetPlayer)
    local character = getCharacter(targetPlayer)

    if not character then
        return nil
    end

    return character:FindFirstChildOfClass("Humanoid")
end

local function getRoot(targetPlayer)
    local character = getCharacter(targetPlayer)

    if not character then
        return nil
    end

    return character:FindFirstChild("HumanoidRootPart")
end

local function isAlive(targetPlayer)
    local humanoid = getHumanoid(targetPlayer)
    local root = getRoot(targetPlayer)

    if not humanoid or not root then
        return false
    end

    return humanoid.Health > 0
end

local function getRemote()
    local character = player.Character

    if not character then
        return nil
    end

    return character:FindFirstChild("Communicate")
end

local function stopOrbit()
    if orbitConnection then
        orbitConnection:Disconnect()
        orbitConnection = nil
    end

    if orbitHumanoid and orbitHumanoid.Parent then
        orbitHumanoid.PlatformStand = oldPlatformStand
    end

    orbitHumanoid = nil
end

local function getSelectedTarget()
    if not selectedTargetName then
        return nil
    end

    return Players:FindFirstChild(selectedTargetName)
end

local function isActive(id, target)
    if not comboEnabled then
        return false
    end

    if workerId ~= id then
        return false
    end

    if not target then
        return false
    end

    return true
end

local function startOrbit(target, id)
    if orbitConnection then
        return
    end

    local humanoid = getHumanoid(player)

    if humanoid then
        orbitHumanoid = humanoid
        oldPlatformStand = humanoid.PlatformStand
        humanoid.PlatformStand = true
    end

    orbitConnection = RunService.Heartbeat:Connect(function(deltaTime)
        if not isActive(id, target) then
            stopOrbit()
            return
        end

        -- Death pauses only. It never disables the toggle.
        if not isAlive(player) or not isAlive(target) then
            stopOrbit()
            return
        end

        local targetRoot = getRoot(target)
        local myRoot = getRoot(player)

        if not targetRoot or not myRoot then
            stopOrbit()
            return
        end

        angle = angle + ORBIT_SPEED * deltaTime

        local orbitPosition = targetRoot.Position + Vector3.new(
            math.cos(angle) * ORBIT_RADIUS,
            ORBIT_HEIGHT,
            math.sin(angle) * ORBIT_RADIUS
        )

        myRoot.CFrame = CFrame.lookAt(orbitPosition, targetRoot.Position)
    end)
end

local function getToolNames()
    local backpack = player:FindFirstChild("Backpack")
    local names = {}

    if not backpack then
        return names
    end

    for _, item in ipairs(backpack:GetChildren()) do
        if item:IsA("Tool") then
            table.insert(names, item.Name)
        end
    end

    table.sort(names)

    return names
end

local function findTool(toolName)
    local backpack = player:FindFirstChild("Backpack")
    local character = player.Character

    local tool = nil

    if backpack then
        tool = backpack:FindFirstChild(toolName)
    end

    if not tool and character then
        tool = character:FindFirstChild(toolName)
    end

    if tool and tool:IsA("Tool") then
        return tool
    end

    return nil
end

local function doM1(id, target)
    for _ = 1, 3 do
        if not isActive(id, target) then
            return false
        end

        if not isAlive(player) or not isAlive(target) then
            return false
        end

        local remote = getRemote()

        if not remote then
            return false
        end

        remote:FireServer({
            Mobile = true,
            Goal = "LeftClick",
        })

        remote:FireServer({
            Mobile = true,
            Goal = "LeftClickRelease",
        })

        task.wait(M1_DELAY)
    end

    return true
end

local function useTool(toolName, id, target)
    if not isActive(id, target) then
        return false
    end

    if not isAlive(player) or not isAlive(target) then
        return false
    end

    local remote = getRemote()
    local tool = findTool(toolName)

    if not remote or not tool then
        return false
    end

    remote:FireServer({
        IsAutoActivate = true,
        Goal = "Console Move",
        Tool = tool,
        ToolName = toolName,
    })

    return true
end

local function doComboRound(id, target)
    local toolNames = getToolNames()
    local toolCount = math.min(#toolNames, 4)

    for index = 1, toolCount do
        if not doM1(id, target) then
            return
        end

        if not useTool(toolNames[index], id, target) then
            return
        end

        if index < toolCount then
            task.wait(TOOL_DELAY)
        end
    end
end

local function startWorker()
    workerId = workerId + 1

    local id = workerId

    task.spawn(function()
        while isActive(id, getSelectedTarget()) do
            local target = getSelectedTarget()

            if target and isAlive(player) and isAlive(target) then
                startOrbit(target, id)
                doComboRound(id, target)
                task.wait(ROUND_DELAY)
            else
                -- Target is dead: wait here until they respawn.
                -- comboEnabled remains true.
                stopOrbit()
                task.wait(DEAD_PAUSE_DELAY)
            end
        end

        stopOrbit()
    end)
end

local function setComboEnabled(enabled)
    comboEnabled = enabled

    workerId = workerId + 1
    stopOrbit()

    if comboEnabled then
        startWorker()
    end
end

local function getPlayerNames()
    local names = {}

    for _, otherPlayer in ipairs(Players:GetPlayers()) do
        if otherPlayer ~= player then
            table.insert(names, otherPlayer.Name)
        end
    end

    table.sort(names)

    return names
end

local Window = WindUI:CreateWindow({
    Title = "Combat Test",
    Icon = "crosshair",
    Size = UDim2.fromOffset(420, 240),
    MinSize = Vector2.new(420, 240),
    Theme = "Dark",
    Transparent = true,
    Resizable = false,
    ToggleKey = Enum.KeyCode.RightShift,
})

local Tab = Window:Tab({
    Title = "Target",
    Icon = "crosshair",
})

local initialNames = getPlayerNames()
selectedTargetName = initialNames[1]

local targetDropdown = Tab:Dropdown({
    Title = "Target player",
    Values = initialNames,
    Value = selectedTargetName,
    Multi = false,
    AllowNone = true,
    Callback = function(playerName)
        selectedTargetName = playerName

        if comboEnabled then
            startWorker()
        end
    end,
})

Tab:Button({
    Title = "Refresh players",
    Callback = function()
        local names = getPlayerNames()

        targetDropdown:Refresh(names)

        if selectedTargetName and Players:FindFirstChild(selectedTargetName) then
            targetDropdown:Select(selectedTargetName)
            return
        end

        selectedTargetName = names[1]

        if selectedTargetName then
            targetDropdown:Select(selectedTargetName)
        end
    end,
})

Tab:Toggle({
    Title = "Combo attack",
    Value = false,
    Callback = function(enabled)
        setComboEnabled(enabled)
    end,
})

Players.PlayerRemoving:Connect(function(leavingPlayer)
    if leavingPlayer.Name == selectedTargetName then
        selectedTargetName = nil
        stopOrbit()
    end
end)

Tab:Select()
